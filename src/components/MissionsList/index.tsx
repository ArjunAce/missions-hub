import styled from "styled-components";

const StyledMissionsList = styled.div`
  width: 100%;
  padding: 32px 24px;
`;

const MissionsList = () => {
  return <StyledMissionsList>MissionsList</StyledMissionsList>;
};

export default MissionsList;
